# Copy to `.env` and adjust. `.env` is gitignored; this file is not.
#
# Scope: the operator plane's database. Every other variable in this repository is
# read at its point of use and documented there — `services/indexer/ponder.config.ts`
# for the indexer's addresses and start block, `packages/keeper/src/cli.ts` for the
# keeper's key and RPC, `apps/*/app/_data.ts` for the surfaces' service URLs, and
# `contracts/script/Rewire.s.sol` for the twelve existing addresses plan 06-13's rewire
# reads (its header carries the one-liner that exports them straight out of
# `contracts/deployments/5042002.json`, so nothing is retyped). Listing them twice
# would guarantee one list goes stale — DEC-55, and the reason no address appears
# below.

# Which host port `docker-compose.yml` publishes Postgres on.
#
# Leave it alone unless 5432 is already taken on your machine — Postgres's default is
# the port every other project also claims, so a clash is the normal case. Compose
# reads this file from the project directory automatically, so setting it here is the
# whole fix; the two URLs below must be edited to match. The alternative — a local
# `-f` override file kept outside the repository — works on exactly one machine and
# does not exist for the next contributor or for CI.
PLAZO_PG_PORT=5432

# The one database. Matches `docker-compose.yml` exactly, so `docker compose up -d
# postgres` and this line are the whole local setup. The port must equal
# `PLAZO_PG_PORT` above.
#
# Its presence is also a switch: `services/origination` serves checkout sessions from
# Postgres when this is set and from process memory when it is not, and it says which
# at startup. Unset it and sessions die with the process.
DATABASE_URL=postgresql://plazo:plazo@localhost:5432/plazo

# Where integration tests may create, fill and drop tables.
#
# Separate from `DATABASE_URL` on purpose: a test suite that truncates is one typo away
# from truncating whatever the developer was looking at. Point it at a different
# database, not a different schema — schema separation is already carrying the
# chain/operator split and should not be asked to carry test isolation as well.
PLAZO_TEST_DATABASE_URL=postgresql://plazo:plazo@localhost:5432/plazo_test

# Opt in to the GOV-08 live witness. Not an address, which is why it is here.
#
# `pnpm --filter @plazo/arc-verify gov08` does nothing without it. GOV-08 itself is
# proven by `forge test --root contracts --mt test_operatorFreeLoop` — that is the gate.
# The live witness is a best-effort extra that renounces ownership irreversibly on a
# throwaway deployment, so it is opt-in rather than part of any standing run, and it
# checks its own funding precondition before it deploys anything.
PLAZO_GOV08=
